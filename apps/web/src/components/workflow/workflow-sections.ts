export { SCRAPE_PAGES, SCRAPE_ROUTES } from '../../config/scrape-panel'

export const WORKFLOW_PAGE = {
  title: 'Batch queue',
  description: 'Submit scrape URLs, watch live progress, and manage batch history',
} as const
