# Panel login + TCP bind — all other service config is in config/ui_config.json (web UI)

PANEL_AUTH_ENABLED=true
PANEL_USERNAME=
PANEL_PASSWORD=

# Server bind (used by `python -m server` and sidebar access IP display)
PANEL_HOST=0.0.0.0
PANEL_PORT=8000
