/** Shared style tokens driven by global UI config CSS variables */
export const uiRadius = {
  borderRadius: 'var(--radius-input)',
} as const

export const uiPanelRadius = {
  borderRadius: 'var(--radius-panel)',
} as const

export const uiShellPadding = {
  px: 'var(--shell-padding-inline)',
} as const
