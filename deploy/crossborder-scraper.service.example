# Example systemd unit — generate with: scraper deploy systemd -o deploy/crossborder-scraper.service
# Install: sudo cp deploy/crossborder-scraper.service /etc/systemd/system/
#         sudo systemctl daemon-reload && sudo systemctl enable --now crossborder-scraper
