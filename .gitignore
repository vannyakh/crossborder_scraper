# Windows reserved device name (accidental redirect)
nul

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Project data (legacy layout at repo root)
data/
*.db

# VPS runtime layout (self-host mutable state)
var/

# Per-plugin install workspaces (legacy at repo root)
installed_plugins/*
!installed_plugins/.gitkeep

installed_skills/*
!installed_skills/README.md
config/agent_skills.yaml

# Secrets
.env
config/ui_config.json
config/agent_schedules.json

# Playwright
playwright-report/
test-results/

.ruff_cache/
.pytest_cache/

.pnpm-store
